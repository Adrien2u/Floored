## What this changes

<!-- One or two sentences. What is different for a person using the app? -->

## Why

<!--
If this contradicts an ADR in docs/adr/, say so here and say why. That is
allowed — the ADRs record reasoning, not law. What is not allowed is
contradicting one silently.
-->

## How it was checked

- [ ] `npm run verify` passes
- [ ] `npx playwright test` passes, or the change cannot affect the browser
- [ ] New behaviour has a test that fails without the change

<!--
For anything touching dimensions, capacity, or clearances: name the source for
every number, the way docs/RESEARCH.md does. A number without a source is the
one kind of change that will be sent back every time.
-->
